# راهنمای استفاده از یورو نوبل در وردپرس و المنتور

این پروژه با **React** توسعه یافته است، اما برای استفاده در وردپرس (WordPress) و المنتور (Elementor) چند روش وجود دارد:

## روش اول: قرار دادن مستقیم در المنتور (تک‌فایل HTML)
من یک فایل به نام `elementor-version.html` در ریشه پروژه ایجاد کرده‌ام. 
1. محتوای این فایل را به طور کامل کپی کنید.
2. در المنتور، یک ویجت **HTML** به صفحه اضافه کنید.
3. کد کپی شده را در آنجا قرار دهید.
*نکته: برای لود شدن استایل‌ها، وردپرس شما باید دسترسی به اینترنت (برای لود Tailwind CDN) داشته باشد.*

## روش دوم: آپلود به عنوان یک اپلیکیشن مستقل
1. از منوی تنظیمات در AI Studio، گزینه **Export to ZIP** را انتخاب کنید.
2. در کامپیوتر خود دستور `npm run build` را اجرا کنید.
3. پوشه `dist` تولید شده را در هاست خود (مثلاً در پوشه `lp`) آپلود کنید.
4. در المنتور از ویجت **iFrame** استفاده کنید و آدرس آن پوشه را بدهید.

## روش سوم: تبدیل به قالب وردپرس (تخصصی)
اگر می‌خواهید این سایت را به یک قالب وردپرس تبدیل کنید که محتوا از پنل وردپرس خوانده شود:
1. باید فایل `index.html` را به `index.php` تبدیل کنید.
2. توابع `wp_head()` و `wp_footer()` را اضافه کنید.
3. استایل‌های Tailwind را با استفاده از فایل CSS نهایی در `functions.php` صف‌بندی (Enqueue) کنید.
